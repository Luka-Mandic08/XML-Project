import AllPurchasedTickets from '../all-purchased-tickets/all-purchased-tickets';

/* eslint-disable-next-line */
export interface FeaturesFlightsPurchasedTicketsContainerProps {}

export function PurchasedTicketsContainer(): React.ReactElement {
  return <AllPurchasedTickets></AllPurchasedTickets>;
}

export default PurchasedTicketsContainer;
